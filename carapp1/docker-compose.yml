version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: carapp-frontend
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    networks:
      - carapp-network

networks:
  carapp-network:
    driver: bridge